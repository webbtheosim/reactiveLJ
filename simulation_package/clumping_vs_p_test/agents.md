I would like to run a test of how frequently "clumping" occurs at different values of $p$ in ReactiveLJ. Essentially, what I want to do is to create a small melt with fixed $\varepsilon_\mathrm{reactiveLJ}=18$ and sweep $p=0$ to $p=9$, inclusive. We will do a single replicate for each $p$ value. The goal will be to initialize a melt using the same protocol as in @simulation_package/melt, except that instead of 4000 chains in a box, we will use only 100 chains (at the same density). However, chains will be smaller to reduce the risk of entanglements so we don't need to equilibrate as long. We'll shrink each chain to length 9, with two linkers per chain, at indices 2 and 6 (zero-indexed). We'll use the same random walk with rejection sampling initialization strategy, the same warmup protocol to activate ReactiveLJ interactions, run an equilibration step of 1e6 steps, then a production stage of 1e6 steps, saving every 1e4 steps to a GSD file as done in @simulation_package/melt. This file will be called generate_clump_test_data.py, and we will write a slurm launcher in @simulation_package/clumping_vs_p_test to run that file, requesting 1 GPU, 1 hour 2 minutes of runtime, and the conda activate '/home/mc7345/micromamba/envs/reactiveLJ_paper' environment.

Next, we will write make_plot.py, which will read the results from the GSD files generated above and make a violin plot (3.3, 3.3, dpi=1000 with violin colors #e77500 and outline color #121212 with axis label font size 10, axis tick font size 8, and no title) showing the distribution of "clump fraction" across all frames for each $p$ value simulated. We will define "clump fraction" as the number of stickers involved in a "clump" divided by the total number of stickers in the frame (which will be the same number for all frames since particle IDs don't change). We will define a "clump" as any sticker that is either (i) "bonded" to two or more stickers, or (ii) "bonded" to a sticker that is "bonded" to two more more stickers. We will define "bonded" as "within the unweakened pairwise LJ inflection radius", as described in Equation 21 in @ReactiveLJ_paper.pdf. Use vectorization and/or numba compilation as appropriate when writing this script to be as efficient as possible. The goal is to visualize the distribution of unphysical clumping as a function of linker stickiness to see what $p$ value is required to inhibit clumping. Save this plot as an SVG. Write a slurm launcher for this script modeled off of @simulation_package/melt/run_analysis.slurm, but adjust the number of CPUs requested to only the number of GSD files simulated and request only 4G of memory and 1 hour 2 minutes of runtime.